version: 2

build:
  os: ubuntu-22.04  # Or another valid OS version
  tools:
    python: "3.8"  # Specify the Python version you are using

sphinx:
  configuration: docs/conf.py  # Path to your Sphinx configuration file

python:
  install:
    - requirements: docs/requirements.txt  # Path to your requirements file

